<analysis>**original_problem_statement:**
The user wants to upload a project from a GitHub repository to continue working on it. The primary goal evolved into fixing persistent and recurring build failures for a Kotlin-based Android application, which is a sophisticated Solana trading bot. After fixing the build, the user wants to generate a test dataset for the bot, run backtests to evaluate its performance, and make improvements based on the results. The final objective is to have a working, compilable application in the GitHub repository.

**PRODUCT REQUIREMENTS:**
1.  Clone the GitHub repository .
2.  Debug and fix all compilation errors in the Android (Kotlin) application.
3.  Ensure the GitHub Actions build passes successfully.
4.  Implement a mechanism for the app to run continuously in the background on a phone.
5.  Generate a 30-day test dataset of the top 30 performing Solana tokens using a free API.
6.  Carry out a backtest of the trading bot against the generated dataset.
7.  Make improvements to the bot's strategy based on backtest results.
8.  Implement a smart AI layer where the bot scales up its buy sizes as the treasury increases.
9.  Build a web dashboard to visualize bot activity.
10. Integrate the Android app with the web dashboard for real-time data synchronization.

**User's preferred language**: English

**what currently exists?**
The initial goal of fixing the Android application's build has been successfully completed. The GitHub Actions workflow now consistently builds a valid APK.

Following this, a full-stack web dashboard has been implemented using React for the frontend and FastAPI for the backend. The dashboard features user authentication (login/register), real-time charts for P&L, statistics cards, and tables for trade history and open positions. The dashboard has been tested and is functional.

However, the focus has shifted back to the Android app, which is now suffering from critical runtime issues. The user reports that the app crashes upon starting the bot, the wallet connection feature is not working, and the token watchlist generates repetitive results.

**Last working item**:
-   **Last item agent was working:** Attempting to fix the wallet connection and app crash issues simultaneously. The last set of changes involved adding extensive, granular logging throughout the   function to pinpoint the exact line of code causing the crash. The wallet connection logic in  and  was also updated with more detailed error logging.
-   **Status:** IN PROGRESS
-   **Agent Testing Done:** N
-   **Which testing method agent to use?** Manual testing by the user is required. The next agent MUST guide the user on how to retrieve on-device logs (e.g., using an app or ) to analyze the crash, as previous attempts to fix it without logs have failed.
-   **User Testing Done:** Y (User confirmed the app still crashes and wallet doesn't connect after the last successful build).

**All Pending/In progress Issue list**:
-   **Issue 1 (P0): App crashes when the bot is started.**
-   **Issue 2 (P0): Wallet connection is not working.**
-   **Issue 3 (P1): Watchlist generates the same tokens repeatedly.**

**Issues Detail:**
-   **Issue 1: App crashes when the bot is started.**
    -   **Attempted fixes:** Multiple layers of  blocks have been added around major components (, , component initializations) in . Null-safe operators () were used to replace force-unwrapping (). The last build (, commit ) added extensive logging to trace the execution flow. Despite these efforts, the user reports the crash persists.
    -   **Next debug checklist:**
        1.  Guide the user to capture device logs (logcat) at the moment of the crash. The extensive logging added in the last build should pinpoint the exact component and line number causing the failure.
        2.  Analyze the logcat output to identify the root .
        3.  Implement a targeted fix based on the specific error found in the logs.
    -   **Why fix this issue and what will be achieved with the fix?** This is a critical blocker. The app is unusable until the crash is resolved.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** Android App only.

-   **Issue 2: Wallet connection is not working.**
    -   **Attempted fixes:** Added detailed error logging to . Modified  and  to dynamically construct the Helius RPC URL if a Helius API key is present, avoiding the rate-limited public RPC. Corrected logic in  to ensure the latest config is reloaded after saving. The issue remains.
    -   **Next debug checklist:**
        1.  Review the logcat from the crash, as the wallet connection failure might be the cause of the crash itself.
        2.  Verify in  and  that the private key and RPC URL from the input fields are correctly saved to  and then immediately reloaded and used for the connection attempt.
        3.  Add toast messages to the UI to display the specific error message from the   block.
    -   **Why fix this issue and what will be achieved with the fix?** The bot cannot perform live trades without a wallet connection.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** Android App only.

-   **Issue 3: Watchlist generates the same tokens repeatedly.**
    -   **Attempted fixes:** The  in  was reduced from 30 minutes to 5-10 seconds to allow tokens to be re-evaluated more frequently. Logic was added to gracefully handle cases where API keys (like Birdeye) are missing, preventing the scanner from relying on a single, limited data source.
    -   **Next debug checklist:** Once the crash is resolved, have the user confirm if the watchlist is more dynamic. If not, log which data sources (, , etc.) are being successfully queried in .
    -   **Why fix this issue and what will be achieved with the fix?** A dynamic watchlist is essential for discovering new trading opportunities.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** Android App only.

**In progress Task List**:
-   None. Current work is focused on fixing the critical P0 bugs.

**Upcoming and Future Tasks**
*   **Upcoming Tasks:**
    *   (P0) Integrate the Android app with the web dashboard for real-time data sync. This is blocked by the app crash.
*   **Future Tasks:**
    *   (P1) Develop the React/FastAPI web dashboard to visualize the bot's trading activity, P&L, and status (This was a future task, now moved to completed).
    *   (P2) Further refine the bot's trading strategy and parameters based on live performance data.
    *   (P2) Add a backtesting UI to the web dashboard.
    *   (P3) Implement mobile-responsive optimizations for the web dashboard.

**Completed work in this session**
-   **Android Build Success:** Fixed over 80 distinct Kotlin compilation errors, leading to a stable and successful GitHub Actions build process. The APK is now being generated correctly.
-   **Web Dashboard Implementation:**
    -   **Backend (FastAPI):** Created  with JWT authentication and API endpoints for , , , , , and .
    -   **Frontend (React):**
        -   Structured the app with  and .
        -   Created multiple reusable dashboard components: , , , , , , .
        -   Implemented a complete, functional UI for visualizing bot data.
    -   **Database (MongoDB):** Set up and used collections for , , , , and .
-   **Testing:** Successfully used the  to perform a comprehensive test of the web dashboard, with all 30 backend API tests and all frontend UI tests passing.
-   **Android Runtime Bug Fixes:**
    -   Fixed a UI bug where the Advanced settings section was not expanding.
    -   Improved the UTC dead zone logic to be less aggressive and allow trading on strong signals.
    -   Fixed an APK installation failure by removing the .

**Code Architecture**


**Key Technical Concepts**
*   **Mobile App:** Android (Kotlin), Gradle, GitHub Actions CI/CD
*   **Backend:** FastAPI, Python
*   **Frontend:** React, JavaScript, , 
*   **Authentication:** JSON Web Tokens (JWT)
*   **Database:** MongoDB
*   **Core Logic:** Solana Trading Bot, Asynchronous programming (Kotlin Coroutines)

**key DB schema**
-   : 
-   : 
-   : 
-   : 
-   : 

**changes in tech stack**
-   None.

**All files of reference**
-   : Newly created backend server.
-   : Main router for the frontend application.
-   : Core UI for the dashboard.
-   : User authentication page.
-   : Critical file for bot lifecycle, repeatedly modified to fix crashes.
-   : Manages wallet state and connection.
-   : Handles low-level wallet operations.
-   : Modified to improve token refresh rate.
-   : Data model for all settings, modified to change refresh interval.

**key api endpoints**
-   **POST** 
-   **POST** 
-   **GET** 
-   **GET** 
-   **GET** 
-   **GET** 
-   **POST**  (For Android app to push data)

**Critical Info for New Agent**
*   **The Android build is STABLE.** Do not attempt to fix build issues. The problem is now at **RUNTIME**.
*   The highest priority is to **diagnose the app crash**. The last build (, commit ) was filled with extensive logging in . You MUST guide the user on how to get device logs () to see these log messages and find the exact . Stop guessing the cause and get the data.
*   The wallet connection failure is likely linked to the crash. It could be failing silently and causing a downstream component to crash. The logs should reveal this.
*   The user's configuration might be the problem. After fixing the crash, verify that the Private Key (Base58) and RPC URL are being saved from the UI, loaded by , and used correctly by .

**documents and test reports created in this job**
-   : Test report for the web dashboard.
-   : Updated with the web dashboard requirements and completion status.

**Last 10 User Messages and any pending HUMAN messages**
1.  **app is still crashing and watchlist generates the same tokens everytime:** User reports the crash from build #10 persists, and the watchlist is stale.
2.  **continue:** User asks the agent to proceed with debugging.
3.  **still doesn't connect the wallet and it crashes:** User reports build #13 still has the crash and wallet connection issues.
4.  **wallet is still not connecting please fix. it worked before. its also blocking buying solana at all:** User re-iterates the P0 wallet issue and a new symptom (trading is blocked).
5.  **Recharged:** User notification.
6.  **I see two issues...** (User screenshot): User provides screenshots showing the crash and UTC dead zone message.
7.  **issues** (User screenshot): User provides the same screenshots again.
8.  **the app is also crashing once you start the bot. also utc time lock may be too aggressive...:** User reports the crash and that the UTC trading pause is too restrictive.
9.  **when I click the advanced link its not building the list:** User reports the Advanced settings toggle is not working.
10. **it installed. the api key settings has no where to enter...:** User successfully installed the APK but cannot find where to input API keys.

**Project Health Check:**
*   **Broken:**
    *   Android App Runtime (crashes on start, wallet non-functional).
*   **Mocked:**
    *   Web dashboard data is currently sample data.

**3rd Party Integrations**
*   **Helius:** RPC and API access. Requires a user-provided API key.
*   **Birdeye:** Token data API. Requires a user-provided API key.
*   **Groq:** LLM for sentiment analysis. Requires a user-provided API key.
*   **CoinGecko:** Used for some market data (free tier).
*   **Telegram:** For notifications. Requires a user-provided token/chat ID.

**Testing status**
-   **Testing agent used after significant changes:** YES (for the web dashboard, which passed).
-   **Troubleshoot agent used after agent stuck in loop:** YES (to analyze the initial 80+ Kotlin errors).
-   **Test files created:** None.
-   **Known regressions:** The wallet connection feature was reportedly working but is now broken.

**What agent forgot to execute**
The agent has repeatedly tried to fix the runtime crash by adding  blocks and making assumptions about the cause. It has failed to take the most critical debugging step: **asking the user to provide on-device logs ()**. The last build was instrumented with extensive logging for this exact purpose, but the agent never followed through to analyze the output. This has led to multiple cycles of failed fixes.</analysis>
